# Pulser Mesh ↔ CMB Engine Correspondence

The ΩaZaTa coordinate system and Pulser Mesh node tuple share a common geometric substrate. This document maps the correspondence explicitly.

## Node Tuple Mapping

| Pulser Mesh | CMB Engine | Notes |
|-------------|------------|-------|
| p (participant identity) | Observer position (θ, φ) | Angular location on boundary |
| q (mission/scarcity vector) | Redshift Za | What's causally accessible |
| φ (phase/commitment) | Harmonic phase φ_phase | Carries antiverse degree of freedom |
| m_s (scarcity vector) | Local density contrast δρ/ρ | Wavefront superposition amplitude |
| g (geography) | Galactic coordinate frame | Physical locality |
| Ξ (continuity score) | Causal connectivity | Fraction of wavefronts intersected |

## Snark Graph ↔ Spacetime Coordinate

The snark graph (personhood threshold requiring memory, discrete approximation of Klein bottle) maps onto the spacetime coordinate node:

- Each coordinate node is a causal bubble — a classical timespace region enabling superposition of intersecting wavefronts
- The Klein bottle approximation: photon and neutrino fields are encoded on the same 2D boundary but carry topologically inequivalent information — the two sides cannot be assigned consistent inside/outside orientation
- The unknown seed as null centroid: the singularity is the fixed point all trajectories approach but none reach

## Triadic Rotor ↔ Two-Layer Field

| Triadic Rotor | CMB Engine |
|---------------|------------|
| sin window | Photon field (CMB, observed) |
| cos window | Neutrino field (CνB, inferred) |
| tan window | Seed signature (divergence between fields) |

The tan window is the singularity's shadow. Maximum divergence between fields marks where the initial asymmetry is most legible.

## Shared Geometry

Both systems operate on hyperbolic space with:
- A boundary condition (CMB surface / Pulser mesh boundary)
- An observer-relative coordinate with conformal scale parameter
- A continuity/connectivity score as a node property
- An imaginary component as a load-bearing degree of freedom
- Asymptotic verifiability within an uncertainty band

These are not analogies. They are the same mathematics instantiated in different domains.
